import TechStackSelector from '../../tech-stack/components/TechStackSelector'

export default function ProfileTechStackSelector({ value, onChange }) {
  return <TechStackSelector value={value} onChange={onChange} />
}
